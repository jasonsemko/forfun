require(['modals'], function(Modals) {

  //This is the global JS file. This file is intentionally blank.
  //This challenge only requires one page so there is nothing technically global yet,
  //however, if in the future this project would need to be expanded up, global logic/
  //could go here.

  var init = function() {};

  init();

});
